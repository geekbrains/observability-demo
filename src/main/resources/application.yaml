spring:
  application:
    name: observability-example

server:
  address: 0.0.0.0
  port: 9999

management:
  endpoints:
    web:
      exposure:
        include: health
  metrics:
    export:
      otlp:
        enabled: true
        endpoint: http://localhost:4318/v1/metrics
        step: 10s
